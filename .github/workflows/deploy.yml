name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    steps:
      - uses: actions/checkout@v4

      - name: Build and push Docker images
        run: |
          echo "Deploy pipeline - configure for your hosting provider"
          echo "Options: Railway, Render, Vercel (frontend), Docker (backend)"
